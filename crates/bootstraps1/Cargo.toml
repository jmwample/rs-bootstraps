[package]
name = "bootstraps1"
version = "0.1.0"
edition = "2021"

[features]
default=["enable-cfg"]
enable-cfg=[]
require-cfg=["enable-cfg"]

[build-dependencies]
config-types = {path="../config-types"}

[dependencies]
config-types = {path="../config-types"}
